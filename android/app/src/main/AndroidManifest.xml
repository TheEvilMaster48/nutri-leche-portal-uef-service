<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nutri_leche">

    <!-- 🌐 Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permite conexión a internet -->
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- Leer contactos del dispositivo -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" /> <!-- Escribir o modificar contactos -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Mostrar notificaciones del sistema -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- Activar vibración para alertas -->
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- Mantener app activa en segundo plano -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- Ejecutar tareas en primer plano -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Ver estado de la red -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- Ver estado del WiFi -->

    <application
        android:label="Nutri Leche Portal"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false" <!-- HTTPS seguro -->
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Tema visible mientras Flutter carga -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Actividad principal -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registro de plugins Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Permite procesar texto desde Flutter -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
